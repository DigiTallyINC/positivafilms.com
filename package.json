{
  "name": "positivafilms-blog-cron",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel-hosted serverless cron that publishes one blog post per fire to the static positivafilms.com repo. The static site itself remains zero-dependency — this package.json exists ONLY for the /api/ functions.",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "@octokit/rest": "^21.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
